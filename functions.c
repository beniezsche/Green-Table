#include "functions.h"

int main() {

    char* cell = "A53";

    getColumn(cell);
    getRow(cell);

    // for(int i = 65; i < 65 + 26; i++) {
    //     printf("%c \n", (char) i);
    // }

    return 0;
}